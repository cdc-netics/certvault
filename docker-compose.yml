version: '3.8'

services:
  mongo:
    image: mongo:8
    container_name: certvault-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=certif-app

volumes:
  mongo_data:
