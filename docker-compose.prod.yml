# Override de producción
# Uso: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  backend:
    image: localhost:5000/certvault-backend:${IMAGE_TAG:-latest}

  frontend:
    image: localhost:5000/certvault-frontend:${IMAGE_TAG:-latest}